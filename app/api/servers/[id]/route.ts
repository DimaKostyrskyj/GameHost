import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { getSessionUserId } from "@/lib/auth";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const uid=await getSessionUserId();if(!uid)return NextResponse.json({error:"Не авторизован"},{status:401});const {id}=await params;try{const r=await db.query(`SELECT * FROM game_servers WHERE id=$1 AND user_id=$2 LIMIT 1`,[id,uid]);if(!r.rows[0])return NextResponse.json({error:"Сервер не найден"},{status:404});return NextResponse.json({server:r.rows[0]});}catch(e){console.error(e);return NextResponse.json({error:"Ошибка базы данных"},{status:500});}}
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const uid=await getSessionUserId();if(!uid)return NextResponse.json({error:"Не авторизован"},{status:401});const {id}=await params;try{const b=await req.json(),name=String(b.name||"").trim().slice(0,64);if(name.length<2)return NextResponse.json({error:"Название слишком короткое"},{status:400});const r=await db.query(`UPDATE game_servers SET name=$1,updated_at=NOW() WHERE id=$2 AND user_id=$3 RETURNING *`,[name,id,uid]);if(!r.rows[0])return NextResponse.json({error:"Сервер не найден"},{status:404});return NextResponse.json({server:r.rows[0]});}catch(e){console.error(e);return NextResponse.json({error:"Ошибка базы данных"},{status:500});}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const uid=await getSessionUserId();if(!uid)return NextResponse.json({error:"Не авторизован"},{status:401});const {id}=await params;try{const r=await db.query(`DELETE FROM game_servers WHERE id=$1 AND user_id=$2 RETURNING id`,[id,uid]);if(!r.rows[0])return NextResponse.json({error:"Сервер не найден"},{status:404});return NextResponse.json({ok:true});}catch(e){console.error(e);return NextResponse.json({error:"Ошибка базы данных"},{status:500});}}
